# 我們要做什麼

Monad Builder Camp Hackathon 專案說明 | 建立 2026-07-29，流程與介面文案 08-04 更新 | 專案名：**Vigil**

> 08-04 更新範圍：技術構成的流程圖、四個情境的介面文案（改成介面上真正的字）、
> 前端形態。**下面「開發大概」那節是 07-29 寫的時程，已經過期，沒有更新**——
> 現在的進度看 [STATUS.md](STATUS.md)。

## 一句話

讓 agent 幫你在 Monad 上執行鏈上操作，簽名之前你看得到它到底要做什麼。

## 這件事現在為什麼難

鏈上交易送到錢包的時候是一段編碼過的資料，人看不懂。畫面上那句「這筆交易會做什麼」，是產生這筆交易的程式自己寫的。程式寫錯或被動過手腳，錯誤會照原樣出現在那句說明裡，讀起來沒有異狀。使用者能做的只有簽或不簽，簽下去不能反悔。

agent 進來之後，中間又多了幾層。使用者說一句話，agent 去挑協議、挑方法、填參數，最後產出一筆交易。使用者看到的是 agent 的轉述，而 agent 自己也可能理解錯。

## 我們的做法

在簽名之前，另外產生一份說明，它的來源不是產生這筆交易的那一方。

做法是把交易丟給 Monad 節點做模擬執行。不真的上鏈，但完整跑一遍，得到「如果現在送出去會發生什麼」的逐條結果。這份結果由節點執行產生，寫交易的程式碰不到它。

畫面上並排放兩件事：**agent 說使用者要什麼**，模擬出來會發生什麼。對得上或對不上，一眼看得到。

「agent 說」這三個字不能省。那句話是 agent 轉述的，我們沒有辦法驗證它等於使用者真正說的話——
能驗的是「交易做的事」對「agent 呼叫的操作」。這條線在下面的流程圖 ④⑤ 之間。

## 使用者會怎麼用

**情況一，正常操作**

使用者說「幫我質押 10 MON」，agent 呼叫工具，面板出現在對話裡：

> 註（08-04）：原本這裡還寫「或者自己在畫面上點 stake、填 10」。**那條路目前沒有實作**——
> 資料契約留了 `source: "panel"` 這個欄位，但正式管線一律填 `"agent"`。要做面板發起
> 是階段二的事，做介面時不要假設它存在。

```
agent 說你要求的
  在 shMONAD 質押 10 MON

鏈上會發生的
  支出  你支出 10 MON
  取得  你取得 9.87 shMON（新鑄出的）

✓ 沒有發現意料外的動作
```

（headline 是「沒有發現意料外的動作」不是「跟你要的一致」。機器沒有驗後者，
寫成一致就是宣稱一件沒被驗證的事。）

每一列可以展開，展開後是未經解讀的原始資料。確認後才簽名。

**情況二，結果跟預期有落差**

使用者以為 unstake 就是錢馬上回來。實際上這個協議送出的是贖回請求，錢晚一點才回得來。

```
agent 說你要求的
  贖回 5 shMON

鏈上會發生的
  支出  你的 5 shMON 被銷毀
  待處理  協議記錄這筆贖回，款項不在這筆交易裡到帳

! 有需要你確認的地方
```

沒有人騙他，但他原本理解的和實際發生的不同。這一頁的作用是讓他在按下去之前知道。

**情況三，對不上**

```
agent 說你要求的
  在 shMONAD 質押 10 MON

鏈上會發生的
  授權  授權 0x9F2c…a41b 動用你的 USDC，沒有上限

✗ 交易內容跟這個操作對不上
  · 多出一筆無上限授權給 0x9F2c…a41b，它不是這個操作指定的對象
  · 沒有任何 MON 被質押
```

這一頁上他該做的只有一件事，不要簽。

**情況四，從外面貼進來的交易**

交易從別的地方來，我們不知道使用者原本要什麼，就只呈現模擬結果，不做比對。如果這個協議我們沒有支援模組，畫面直接顯示原始的資產變動，並且說明這是未經解讀的資料。

> 註（08-04）：**這個情境目前也沒有實作路徑**。渲染層、文案、fixture 都做好了，
> 但正式管線每一次都會建出 intent，所以「沒有可對照的東西」這個狀態實際跑不出來。
> 介面要照做（它是階段二的規劃），但知道它現在只在預覽頁看得到。
>
> 「沒有支援模組」那半是真的會發生，那條有實作。

## 技術構成

```
① 你講了一句話
   agent 把它轉述一遍，呼叫我們的工具。
   那句轉述是 agent 說的，沒有任何人驗過。
        │
        ▼
② 建出交易
   由 Moss 組出一筆還沒簽名的交易。
   組交易的這一方，我們一樣假設它可能出錯或被動手腳。
        │
        ▼
③ 拿去模擬
   丟給 Monad 節點完整跑一遍，但不上鏈。
   不花錢、不用簽名、鏈上狀態不會變。
        │
        ▼
④ 查核與比對（機器做）
   確認報告沒有漏掉、重複或捏造任何一條變動；
   用五條規則比「交易做的事」對「agent 呼叫的操作」；
   另外拿真實餘額對一次，看付不付得起；
   把 wei 換成人看得懂的數字。
        │
        ▼
⑤ 你自己比（人做）
   畫面並排「agent 說你要求的」與「鏈上會發生的」。
   這一層不交給 AI 判斷。
        │
        ▼
⑥ 你決定
   要簽的話，畫面直接交給簽名頁，不繞回 agent。
   兩頁顯示同一串指紋讓你對照，錢包還會比對帳戶。
```

**④ 和 ⑤ 的分界是這個產品最重要的一條線。** 機器比得了「交易 vs agent 呼叫的操作」，
比不了「交易 vs 你說的那句話」——後者是自然語言，機器判斷不了。所以介面上不能寫
「跟你要的一致」，只能寫「沒有發現意料外的動作」，然後把兩段並排讓人自己看。

寫文案、做介面的時候這條線不能模糊掉。說得比實際驗過的多，這個產品就變成另一個
「畫面說沒問題」的東西，而那正是我們要解決的問題。

底層用 Moss，一個開源的鏈上操作與模擬框架，負責協議理解、交易組建、模擬與結果解析。
我們做的是它上面的使用介面與比對邏輯。**Moss 也在不可信的那一邊**——證據來自節點執行，
不來自 Moss 的說法。

前端形態：**渲染在 AI 對話裡的面板（MCP App）**，另外 CLI agent 拿到內容等價的文字面板。
（原本規劃的瀏覽器插件側邊欄已於 07-31 砍掉，理由見 STATUS.md 的決定紀錄。）

## 幾件已經定下的事

- 跑 Monad 主網。Moss 目前只支援主網，模擬本身不花錢也不送出交易，demo 真正簽名時用小額。
- 模擬過程只要出現警告，就不允許簽名。
- 每一條證據都要能展開到原始資料。上面那層人話是解讀，底下那層是節點跑出來的，兩層都要看得到。
- 沒有支援模組的協議，顯示原始變動就好，不要猜。
- 示範用兩個協議：Kuru、shMONAD。

## 開發大概

**第一階段（7/29 起）**
定下資料格式，做出涵蓋各種情況的假資料。介面先對著假資料做，模擬管線同時接通，兩邊不互相等。

**第二階段（8/2）**
第一次整合，真實的模擬結果接進介面。這一天留出來處理接不上的問題。

**第三階段（8/3 到 8/6）**
接上 agent、意圖比對、瀏覽器插件形態、外部交易貼入。

**第四階段（8/7 到 8/8）**
對主網跑通完整流程，錄製，整理提交材料。

8/9 下午 Project Open Day 報告。

## 名詞

| 詞 | 意思 |
|---|---|
| calldata | 交易攜帶的原始指令資料，決定上鏈後執行什麼 |
| 模擬執行 | 讓節點完整跑一遍交易但不上鏈，看會發生什麼 |
| debug_traceCall | Monad 節點提供的模擬執行方法 |
| Moss | 開源的鏈上操作與模擬框架，我們的底層 |
| Change | 模擬跑出來的逐條變動，未經解讀 |
| Receipt | 把 Change 翻成人看得懂的報告 |
| adapter | 讓 Moss 看懂某個協議的模組，一個協議一個 |
| agent | 幫使用者理解需求並組出操作的 AI |
| MCP | agent 呼叫外部功能的介面規格，Moss 用它把能力開放給 agent |
| shMONAD | Monad 上的質押協議 |
| Kuru | Monad 上的交易協議 |
